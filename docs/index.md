# Spam Mail Detector

Welcome — this repository contains a simple spam detector (SMS/email) implemented with scikit-learn.

- API: `/predict` endpoint served by Flask in `app.py`.
- Train: `src/train.py` saves `models/spam_classifier.joblib`.

Visit the repository for code, CI, and deployment workflows.
